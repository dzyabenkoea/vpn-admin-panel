{% extends 'base.j2' %}
{% block content %}

<div class="flex flex-col gap-4">
    <div class="flex justify-between">
        <h1 class="text-2xl">Пользователи</h1>
        <input type="search" placeholder="Поиск" class="border px-2">
    </div>
    <button class="bg-gray-800 text-white px-4 py-2">Добавить</button>
    <div class="flex flex-col gap-2">
        {% for user in users %}
        <section class="flex items-center justify-between border px-4 py-2">
            <span class="font-semibold">{{ user.name }}</span>
            <div class="flex flex-row items-center gap-2">
                <button class="get-link-button">Получить ссылку</button>
                <button class="remove-button text-red-500">Удалить</button>
            </div>
        </section>
        {% endfor %}
    </div>
</div>
<script>
    const getUserLinkButton = document.querySelector('.get-link-button')
    const removeUserButton = document.querySelector('.remove-button')

    getUserLinkButton.addEventListener('click', getUserlinkOnClick)
    removeUserButton.addEventListener('click', removeUserButtonOnClick)


    function getUserlinkOnClick(event) {
        
    }

    function removeUserButtonOnClick(event) {
        
    }

</script>
{% endblock content%}